// ══════════════════════════════════════════════════════════════
// Paste your Firebase project's config values here.
// Find them at: Firebase Console → Project Settings (gear icon)
// → scroll to "Your apps" → the web app you registered.
// This is the ONLY file you need to edit with your real keys —
// both admin.html and schedule.html import it from here.
// ══════════════════════════════════════════════════════════════
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
